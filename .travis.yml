language: elixir
elixir:
  - 1.6.0
otp_release:
  - 20.3
services:
  - postgres
script:
  - mix deps.get
  - MIX_ENV=test mix do compile --warnings-as-errors
  - mix credo --strict
  - mix dialyzer
  - MIX_ENV=test mix coveralls.travis --umbrella

